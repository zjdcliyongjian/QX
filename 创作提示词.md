# 七夕浪漫 3D 爱心粒子：创作提示词

下面这份提示词可以交给具备编程能力的 AI，用来从零生成类似的桌面动画。可根据需要替换文字、颜色、音乐和时间线。

## 完整提示词

```text
请为我制作一个名为“七夕浪漫3D爱心粒子”的桌面动画项目，并提供完整、可二次开发的源码。

一、目标平台
1. 同时支持 Windows 和 macOS。
2. Windows 版本输出单文件 EXE，双击后全屏播放。
3. macOS 版本输出 .app，并提供 DMG 和 ZIP 安装包。
4. 右上角必须有清晰但不抢画面的圆形关闭按钮；Windows 和 macOS 都支持按 Esc 退出。
5. 动画无需网络即可运行，不使用外部图片作为核心画面。

二、视觉方向
1. 整体气质是浪漫、梦幻、克制，不要大红色婚庆风，也不要廉价霓虹灯效果。
2. 背景使用深靛蓝到暗紫色的夜空渐变，加入稀疏星点、中央柔和紫光和两条透明度很低的 S 形极光。
3. 主色为紫色、玫粉色和少量珍珠白高光：
   - 夜空：#040716、#09091F、#1A0B31
   - 紫色粒子：#8E67FF、#A06BFF
   - 粉色爱心：#B8409B、#F35AA8、#FF65B4
   - 高光：#FFE9F8
4. 爱心必须是全画面的唯一视觉中心，背景极光不能穿透文字形成干扰。

三、10 秒动画时间线
1. 0.0–0.8 秒：紫色粒子盘在画面下方出现，粒子盘呈透视椭圆，并做非常轻微的双层反向旋转。
2. 0.3–1.6 秒：一部分紫色粒子从粒子盘向上喷涌，只向上运动，不向下落。
3. 0.7–4.2 秒：上升粒子逐渐向画面中央汇聚，先勾勒爱心轮廓，再填充内部；粒子颜色由紫色逐渐过渡到玫粉色。
4. 4.2 秒左右：爱心快速完成一次轻微心跳；爱心成型后，内部粒子仍继续逐渐增多。
5. 4.3–4.9 秒：爱心中央淡入“七夕快乐”，文字保持正面朝向，不跟随 3D 模型旋转。
6. 5.2–8.7 秒：三维爱心围绕 Y 轴缓慢旋转，通过透视、粒子大小、亮度和深度差表现爱心球的体积。
7. 8.75–9.92 秒：整体平滑淡出。
8. 10 秒：无跳帧重新循环。

四、粒子行为
1. 下方粒子盘由大量紫色微粒构成，形状是有纵深的椭圆光盘，旋转必须轻微，不能高速旋转。
2. 上升粒子具有不同大小、速度、横向摆动和生命周期；少量珍珠白大粒子提供高光。
3. 上升粒子到达爱心区域后淡出或并入爱心，不允许像雨点一样重新下落。
4. 爱心至少包含数千个粒子。可使用经典参数方程生成二维心形骨架：
   x = 16 × sin³(t)
   y = 13 × cos(t) - 5 × cos(2t) - 2 × cos(3t) - cos(4t)
5. 在二维心形上增加 z 轴随机厚度，并结合 Y 轴旋转、透视缩放、前亮后暗、近大远小，形成三维爱心球。
6. 粒子亮度需要轻微呼吸和闪烁，但不要所有粒子同步闪烁。

五、文字与交互
1. 爱心中央显示“七夕快乐”，使用中文行楷或楷体，白粉色文字配低强度粉色外发光。
2. 文字不能被高亮粒子完全遮挡，并且始终保持可读。
3. 右上角关闭按钮使用半透明深色底、细白色描边和白色叉号。
4. 支持可选音乐：如果程序同目录存在“传奇.mp3”就循环播放；不存在时动画正常运行。

六、工程与交付
1. 提供结构清楚、有注释、方便修改颜色、粒子数量、时间线和文字的完整源码。
2. Windows 可使用 C# WinForms + GDI+；macOS 可使用 Swift AppKit + WKWebView + Canvas。
3. 提供 Windows 和 macOS 的本地构建脚本。
4. 提供 GitHub Actions，在 Windows 与 macOS 官方运行器上分别构建，并上传成品。
5. 提供 README、使用说明、.gitignore 和一张真实运行预览图。
6. 不要把音乐、参考视频、临时截图、编译目录或个人路径提交到仓库。

完成后请检查：10 秒循环、全屏、关闭按钮、Esc、粒子只向上、爱心成型速度、成型后增密、3D 旋转、文字可读性，以及 Windows/macOS 构建产物是否存在。
```

## 二创时最常改的地方

- 中央文字：搜索 `七夕快乐`。
- 动画时长：搜索 `10`、`8.75` 和 `9.92` 附近的时间线参数。
- 爱心粒子数量：Windows 源码查看爱心粒子集合；macOS 源码查看 `heart` 数组长度。
- 配色：搜索 `#8E67FF`、`#F35AA8` 和 `#FF65B4`。
- 爱心旋转起点：搜索 `5.2`。
- 音乐文件名：搜索 `传奇.mp3`。
